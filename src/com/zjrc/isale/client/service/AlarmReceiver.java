package com.zjrc.isale.client.service;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class AlarmReceiver extends BroadcastReceiver {

	@Override 
	public void onReceive(Context context, Intent intent) {
//        if (intent.getAction()!=null && intent.getAction().equals("com.zjrc.isale.client.alarm.action")) {
//        	Log.i("AlarmReceiver", "AlarmReceiver start LocationService...");
//            Intent intentservice = new Intent();
//            intentservice.setClass(context, LocationService.class);
//            context.startService(intentservice);
//        }
	}

}
